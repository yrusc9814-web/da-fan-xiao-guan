# agent.md

## 1. 文档定位

本文件是 **“搭饭小馆”** 项目的最高开发执行规范，供 Codex、开发 Agent 和后续维护任务使用。

所有开发任务开始前，必须依次阅读：

```text
1. agent.md
2. docs/PRD.md
3. docs/design/desktop-home-reference.png
4. docs/design/mobile-home-reference.png
```

执行优先级：

```text
工程、安全、数据和执行流程：以 agent.md 为准
功能、字段和业务逻辑：以 PRD 为准
首页视觉、布局比例和小羊风格：以最终参考图为准
```

本版已经针对最终桌面端和移动端参考图完成冲突消解。

---

## 2. 项目定义

### 2.1 项目名称

项目主名称固定为：

```text
搭饭小馆
```

品牌副标题固定为：

```text
让每一餐都更美好
```

禁止继续使用以下名称作为产品主名称：

```text
今天吃什么
饮食决策助手
食光小满
```

参考图中的用户昵称“小满”仅为演示数据，不得硬编码。真实昵称从系统设置读取。

### 2.2 产品形态

“搭饭小馆”是运行在 Windows 本地电脑上的个人饮食决策与记录系统。

必须支持：

- Windows 电脑网页端
- 手机网页端
- 同一局域网访问
- 本地 SQLite 数据库
- 本地图片文件
- 浏览器关闭后数据保留
- 服务重启后数据保留
- 电脑重启后数据保留
- 无 Docker
- 无 PostgreSQL
- 无云数据库
- 无强制联网依赖

使用范围：

- 个人
- 情侣
- 家庭内部

不建设复杂账号、注册、短信、邮箱和多角色权限系统。

---

## 3. 核心业务闭环

系统必须实现真实闭环：

```text
录入菜谱、店铺、食材和厨房工具
        ↓
根据条件、库存和偏好生成推荐
        ↓
加入每日饮食计划
        ↓
完成计划
        ↓
生成饮食日记草稿
        ↓
确认库存扣减
        ↓
更新日历、历史和统计
        ↓
缺少食材可加入购物清单
```

禁止只做静态页面、空按钮或前端 Mock。

---

## 4. 最终参考图规则

最终参考图文件统一命名为：

```text
docs/design/desktop-home-reference.png
docs/design/mobile-home-reference.png
```

### 4.1 参考图决定的内容

参考图是首页视觉验收基准，决定：

- 页面整体粉白色系
- 小羊形象及插画风格
- 卡片圆角、边框和阴影
- 桌面端首页模块顺序
- 移动端首页模块顺序
- 桌面端多列信息密度
- 移动端两列快捷入口
- 推荐菜谱卡片比例
- 库存、记录、统计和日历的排版
- 留白和视觉层级

### 4.2 参考图不决定的内容

以下内容不得机械照搬：

- 演示昵称
- 演示日期
- 演示菜名
- 演示库存数字
- 错别字
- 不真实数据
- 不完整交互
- 生成图产生的文字变形
- 手机设备外壳
- 浏览器边框
- 会员付费入口

### 4.3 小羊素材规范

小羊必须与最终参考图保持同一角色风格：

- 白色蓬松卷毛
- 奶茶色卷角
- 粉色耳朵
- 大眼睛
- 粉色腮红
- 暖粉色小围兜或杯子
- 线条柔和
- 轻立体插画质感
- 粉白暖色调
- 可爱但不过度幼儿化

必须建立统一素材：

```text
src/assets/mascot/brand-logo.png
src/assets/mascot/home-hero.png
src/assets/mascot/sidebar-tip.png
src/assets/mascot/empty-state.png
```

要求：

- 所有小羊来自同一角色设定。
- 不同页面不得出现明显不同画风的小羊。
- 不得使用随机 Emoji 代替主视觉小羊。
- 不得运行时调用 AI 生成小羊。
- 小羊素材必须随应用本地打包。
- Logo、首页横幅和空状态可以采用不同姿势，但角色特征必须一致。

---

## 5. 首页排版锁定

## 5.1 桌面端首页

桌面端采用：

```text
左侧固定导航
+
右侧主内容区
```

桌面端首页从上到下固定为：

```text
1. 顶部搜索、通知和用户区
2. 问候横幅及小羊插画
3. 四个核心快捷入口
4. 今日推荐菜谱
5. 今日饮食记录 + 本周统计 + 库存概览
6. 饮食日历
```

### 第一行：顶部区域

包含：

- 搜索框
- 通知入口
- 可选日历快捷入口
- 用户头像
- 用户昵称
- 用户菜单

### 第二行：问候横幅

包含：

- `早上好 / 下午好 / 晚上好`
- 用户昵称
- 简短副文案
- 右侧小羊插画
- 少量云朵、爱心、花盆或清单装饰

问候语必须根据本地时间动态变化，不能固定为“早上好”。

### 第三行：四个快捷入口

固定为一行四列：

```text
随机决定
搭配一顿饭
库存消耗
库存推荐
```

每张卡片包含：

- 标题
- 两行以内说明
- 本地插画
- 主操作按钮

### 第四行：今日推荐菜谱

必须横向占满主内容区。

要求：

- 不再与库存概览并排。
- 推荐区占据原本库存概览所在的横向宽度。
- 桌面端默认展示 5–6 张菜谱卡。
- 卡片显示图片、名称、标签、时长和评分。
- 右上角提供“查看更多”。
- 数量超出时可横向滚动或分页。

### 第五行：三列信息卡

顺序固定为：

```text
左：今日饮食记录
中：本周统计
右：库存概览
```

禁止出现第二个“今日饮食记录”。

库存概览必须位于本行右侧，内容包含：

- 食材总数
- 即将过期数量
- 库存不足数量
- 即将过期食材列表
- 查看全部入口

### 第六行：饮食日历

饮食日历横向占满主内容区。

包含：

- 当前日期范围
- 上一周期
- 下一周期
- 当天高亮
- 计划、记录、推荐或收藏状态标记

### 桌面端侧栏底部

参考图中的“会员专享、升级会员”不属于本项目。

实际实现必须替换为：

```text
搭饭小贴士
```

该区域只能用于：

- 小羊插画
- 本地饮食提示
- 库存提醒
- 今日计划提示

禁止：

- 会员体系
- 付费升级
- 订阅
- 广告
- 商业推广

---

## 5.2 移动端首页

移动端是真实响应式网页，不包含手机外壳。

从上到下固定为：

```text
1. 品牌栏
2. 问候横幅
3. 四个核心快捷入口
4. 今日推荐菜谱
5. 库存概览
6. 今日饮食记录
7. 本周统计
8. 饮食日历
9. 底部四栏导航
```

### 品牌栏

包含：

- 小羊 Logo
- 搭饭小馆
- 让每一餐都更美好
- 右侧设置入口

### 问候横幅

包含：

- 动态问候语
- 用户昵称
- 副文案
- 小羊插画

### 快捷入口

采用两列两行宫格：

```text
随机决定
搭配一顿饭
库存消耗
库存推荐
```

### 今日推荐菜谱

使用横向滑动卡片。

要求：

- 一屏显示约 2.3–3.3 张卡片。
- 滑动时保留下一张卡片露出部分。
- 不压缩成无法阅读的小卡片。
- 支持点击进入详情。

### 库存概览

移动端库存概览为一张完整卡片，内部左右或上下组合：

```text
库存统计
+
即将过期的食材
```

不得将“即将过期的食材”做成无标题的独立浮块。

### 今日饮食记录

只出现一次。

内容包含：

- 早餐
- 午餐
- 晚餐
- 完成数量
- 新增按钮

### 本周统计

使用单张横向统计卡。

显示：

- 记录天数
- 总餐次
- 平均评分
- 食材消耗

### 饮食日历

移动端使用横向一周视图。

### 底部导航

固定为：

```text
首页
厨师
觅食
日记
```

设置不占用底部导航，放在品牌栏右上角。

---

## 6. 导航与信息架构

桌面端和移动端可以使用不同的导航组织方式，但必须访问同一套业务数据。

## 6.1 桌面端侧栏

桌面端侧栏按任务直接展开：

```text
首页
菜谱推荐
饮食记录
食材库存
饮食日历
统计分析
收藏夹
购物清单
设置
```

说明：

### 菜谱推荐

内部使用标签页：

```text
自己做
到店
外卖
```

因此店铺和觅食功能仍然保留，不需要额外增加一个桌面端“觅食”一级菜单。

### 饮食记录

包含：

- 饮食日记
- 历史记录
- 新增记录
- 评分和备注

### 食材库存

内部使用标签页：

```text
食材
厨房工具
库存日志
库存推荐
剩菜消耗
```

### 饮食日历

包含：

- 每日计划
- 周计划
- 餐次安排
- 计划完成状态

### 统计分析

实现 PRD 中的基础饮食统计。

禁止仅根据参考图名称擅自增加专业营养诊断。

没有完整营养数据时，不展示：

- 卡路里处方
- 医疗建议
- 专业营养评估
- 健康风险诊断

### 收藏夹

使用标签页：

```text
菜谱
店铺
套餐
```

### 购物清单

购物清单纳入第一版轻量功能。

支持：

- 手动新增
- 根据缺少食材生成
- 根据低库存生成
- 勾选完成
- 修改数量和单位
- 清空已完成

不接入电商平台，不提供购买跳转。

### 设置

包含：

- 项目名称和副标题
- 用户昵称和头像
- 访问 PIN
- 默认推荐条件
- 食用者管理
- 数据备份与恢复
- 数据导出
- 局域网访问说明
- 可选 AI 配置

## 6.2 移动端底部栏目

### 首页

- 首页总览
- 随机决定
- 搭配一顿饭
- 推荐结果
- 今日计划

### 厨师

- 菜谱
- 食材
- 厨房工具
- 库存推荐
- 剩菜消耗
- 购物清单

### 觅食

- 到店
- 外卖
- 店铺收藏
- 店铺管理

### 日记

- 饮食记录
- 饮食日历
- 统计分析
- 食用者

---

## 7. 技术栈

### 前端

必须使用：

```text
Vue 3
TypeScript
Vite
Vue Router
Pinia
ECharts
```

允许使用：

- CSS Variables
- 原生 CSS
- 轻量图标库
- 必要的图片压缩库

不建议使用大型 UI 框架。

如使用组件库，必须满足：

- 可以彻底覆盖默认样式
- 不出现明显 Material Design 风格
- 不破坏粉白视觉体系
- 不限制移动端布局
- 不显著增加部署复杂度

### 后端

必须使用：

```text
Node.js
TypeScript
Fastify
Prisma
SQLite
```

### 主数据库

固定为：

```text
data/app.db
```

禁止使用：

- PostgreSQL
- MySQL
- MongoDB
- Docker
- Docker Desktop
- 云数据库
- JSON 文件作为业务数据库
- localStorage 作为业务数据库

localStorage 只允许保存非关键 UI 偏好和临时会话状态。

---

## 8. 推荐项目目录

```text
food-app/
├─ app/
│  ├─ client/
│  │  ├─ src/
│  │  │  ├─ assets/
│  │  │  │  └─ mascot/
│  │  │  ├─ components/
│  │  │  ├─ composables/
│  │  │  ├─ layouts/
│  │  │  ├─ pages/
│  │  │  ├─ router/
│  │  │  ├─ services/
│  │  │  ├─ stores/
│  │  │  ├─ styles/
│  │  │  ├─ types/
│  │  │  ├─ utils/
│  │  │  ├─ App.vue
│  │  │  └─ main.ts
│  │  ├─ index.html
│  │  └─ vite.config.ts
│  │
│  ├─ server/
│  │  ├─ src/
│  │  │  ├─ config/
│  │  │  ├─ db/
│  │  │  ├─ modules/
│  │  │  ├─ plugins/
│  │  │  ├─ shared/
│  │  │  ├─ app.ts
│  │  │  └─ server.ts
│  │  └─ prisma/
│  │     ├─ migrations/
│  │     └─ schema.prisma
│  │
│  └─ shared/
│     └─ types/
│
├─ data/
│  ├─ app.db
│  ├─ uploads/
│  ├─ backups/
│  └─ app.pid
├─ config/
│  └─ config.json
├─ docs/
│  ├─ PRD.md
│  └─ design/
│     ├─ desktop-home-reference.png
│     └─ mobile-home-reference.png
├─ logs/
├─ scripts/
├─ tests/
│  ├─ api/
│  ├─ integration/
│  └─ ui/
├─ start.bat
├─ stop.bat
├─ README.md
└─ agent.md
```

---

## 9. 开发阶段与并行规则

总体流程：

```text
阶段 0：资料和仓库准备
阶段 1：基础工程
阶段 2：数据库与 API 底座
阶段 3：设计系统和响应式框架
阶段 4：首页双端开发与视觉验收
阶段 5：业务模块有限并行
阶段 6：公共能力补全
阶段 7：模块集成
阶段 8：业务闭环
阶段 9：Windows 部署
阶段 10：备份恢复
阶段 11：全量测试
阶段 12：最终验收
```

规则：

```text
阶段 0–4：串行
阶段 5：最多四路并行
阶段 6–12：统一集成后串行
```

阶段 4 未通过双端首页视觉验收前，不得开始业务模块并行。

### 并行工作流

#### A：菜谱、食材与厨房

- 菜谱
- 食材
- 厨房工具
- 库存日志
- 库存推荐
- 剩菜消耗
- 图片上传

#### B：店铺与觅食

- 店铺
- 到店
- 外卖
- 店铺收藏
- 搜索和筛选

#### C：计划、推荐与购物清单

- 每日计划
- 餐次
- 随机决定
- 搭配一顿饭
- 推荐历史
- 购物清单

#### D：日记、食用者与统计

- 饮食记录
- 日历
- 食用者
- 周月统计
- 排行和趋势

### 并行锁定文件

并行开发期间，以下文件只允许总集成任务修改：

```text
prisma/schema.prisma
prisma/migrations/**
package.json
App.vue
router/**
styles/tokens.css
styles/global.css
shared/types/**
公共 API 返回结构
公共错误码
```

其他工作流只能提出修改建议，不得直接覆盖。

---

## 10. 数据库规则

第一版至少包含：

```text
settings
recipes
recipe_steps
tags
recipe_tags
recipe_meal_types
recipe_tools
ingredients
inventory_logs
kitchen_tools
stores
diners
meal_plans
meal_plan_diners
meal_records
meal_record_diners
recommendation_history
shopping_lists
shopping_list_items
deleted_items
```

### Prisma

必须使用正式 Migration。

禁止：

- 手工修改正式数据库结构
- 使用不可追踪 SQL
- 删除已有迁移
- 使用 `prisma db push` 替代正式迁移
- 多个并行工作流分别修改 Schema

### 删除

默认软删除：

- 菜谱
- 店铺
- 食材
- 厨房工具
- 饮食记录
- 购物清单

有关联历史的食用者只能停用。

### 事务

以下操作必须使用事务：

- 保存菜谱和步骤
- 完成计划并生成日记
- 完成计划并扣减库存
- 批量调整库存
- 根据缺少食材生成购物清单
- 删除带关联数据的实体
- 恢复备份

### 时间

系统时间字段保存 UTC。

业务日期保存：

```text
YYYY-MM-DD
```

不得通过 UTC 自动推导业务日期。

---

## 11. API 规则

统一前缀：

```text
/api/v1
```

成功：

```json
{
  "success": true,
  "data": {},
  "error": null
}
```

失败：

```json
{
  "success": false,
  "data": null,
  "error": {
    "code": "RECIPE_NOT_FOUND",
    "message": "菜谱不存在"
  }
}
```

必须使用正确 HTTP 状态码。

不得向前端暴露：

- 数据库路径
- 服务端堆栈
- PIN
- API Key
- 本地文件绝对路径

---

## 12. 推荐系统规则

第一版使用本地规则，不依赖 AI。

候选来源：

- 菜谱
- 到店店铺
- 外卖店铺
- 收藏内容
- 库存匹配菜谱

硬条件：

- 餐次
- 获取方式
- 不想吃
- 重复周期
- 食用者忌口
- 食用者过敏
- 必须消耗食材
- 必须具备工具
- 仅使用现有食材

软权重：

- 想吃
- 收藏
- 评分
- 库存匹配度
- 即将过期
- 最近未吃
- 最近未推荐
- 新尝试
- 历史接受率

推荐结果必须说明：

- 候选数量
- 推荐原因
- 命中条件
- 库存匹配
- 最近食用时间
- 是否新尝试
- 缺少食材
- 是否可加入购物清单

每次推荐必须写入推荐历史。

---

## 13. 响应式规则

断点：

```text
手机：0–767px
平板：768–1199px
桌面：1200px 及以上
```

### 手机

- 页面主区块纵向单列。
- 快捷入口固定两列。
- 菜谱横向滑动。
- 小型数据允许横向排列。
- 底部导航固定。
- 页面左右边距建议 16px。
- 交互目标不小于 44px。
- 避开安全区。
- 不依赖 hover。
- 不显示手机外壳。
- 360px 宽度无横向溢出。

### 桌面

- 左侧固定导航。
- 主内容最大宽度建议 1440px。
- 今日推荐菜谱必须占满一行。
- 今日记录、本周统计、库存概览固定为三列。
- 日历占满一行。
- 不显示移动端底栏。
- 不把移动端页面直接拉宽。
- 长表单使用右侧抽屉。
- 简短表单使用居中弹窗。

---

## 14. 视觉规范

### 主色

```css
--color-primary: #F47F9F;
--color-primary-hover: #E96E91;
--color-primary-soft: #FDE4EB;
--color-page-bg: #FFF8FA;
```

### 辅助色

```css
--color-green: #9FD3B1;
--color-green-soft: #EAF7EF;
--color-orange: #F3B27B;
--color-orange-soft: #FFF1E8;
--color-purple: #C4B0F2;
--color-blue-soft: #EAF3FF;
```

辅助色只能用于：

- 标签
- 状态
- 快捷入口插画底色
- 图表区分

主界面仍以粉白色为主。

### 中性色

```css
--color-text-primary: #2F282B;
--color-text-secondary: #756D70;
--color-text-muted: #AAA1A5;
--color-border: #F1DDE4;
--color-card: #FFFFFF;
```

### 圆角

```css
--radius-tag: 999px;
--radius-input: 14px;
--radius-card: 18px;
--radius-card-large: 22px;
--radius-dialog: 24px;
```

### 阴影

```css
--shadow-card: 0 8px 24px rgba(92, 58, 70, 0.06);
```

禁止：

- 深色主题作为默认主题
- 绿色或米黄色作为主色
- 强烈渐变
- 黑色重阴影
- 霓虹发光
- 复杂 3D
- 过度玻璃拟态
- Material Design 照搬
- 不同画风的卡通素材混用

---

## 15. 图片规则

业务图片存放：

```text
data/uploads
```

数据库只保存相对路径。

支持：

```text
JPG
PNG
WEBP
```

要求：

- 单张最大 8MB
- 长边压缩至 1920px
- 自动生成缩略图
- 服务端重新命名
- 校验 MIME 和扩展名
- 防止路径穿越
- 删除前检查引用
- 禁止长期使用 Base64 保存到 SQLite

项目内置小羊素材存放于前端静态资源，不进入业务上传目录。

---

## 16. Windows 本地运行

默认端口：

```text
8787
```

监听：

```text
0.0.0.0
```

只用于本机和局域网。

禁止：

- 公网部署
- 自动路由器映射
- 公网穿透
- 云服务器依赖

### start.bat

必须：

1. 检查 Node 环境。
2. 检查端口。
3. 检查数据目录。
4. 检查数据库。
5. 首次启动执行迁移。
6. 启动服务。
7. 保存 PID。
8. 自动打开电脑浏览器。
9. 显示电脑地址。
10. 显示手机地址。
11. 保存日志。

### stop.bat

只能停止 PID 文件对应的本项目进程。

禁止结束所有 `node.exe`。

---

## 17. 备份恢复

自动备份：

```text
每天首次启动一次
保留最近 30 份
```

手动备份 ZIP 至少包含：

```text
app.db
uploads/
config.json
backup-manifest.json
```

恢复前必须：

1. 校验压缩包。
2. 校验 manifest。
3. 校验 SHA-256。
4. 备份当前数据。
5. 暂停写入。
6. 临时目录解压。
7. 验证数据库可打开。
8. 原子替换。
9. 失败回滚。

---

## 18. 页面状态

所有真实页面必须处理：

- 加载中
- 加载失败
- 空数据
- 保存中
- 保存成功
- 保存失败
- 删除确认
- 网络断开
- 后端未启动
- 数据库异常

要求：

- 保存失败保留表单内容。
- 提交时禁止重复点击。
- 推荐无结果时说明原因。
- 不向普通用户展示技术堆栈。
- 不使用假数据掩盖异常。

---

## 19. 测试与视觉验收

### 单元测试

至少覆盖：

- 推荐硬过滤
- 推荐权重
- 重复周期
- 忌口和过敏
- 库存匹配
- 到期计算
- 统计计算
- 购物清单生成
- 文件路径校验
- PIN 验证

### API 测试

至少覆盖：

- 菜谱 CRUD
- 食材 CRUD
- 工具 CRUD
- 店铺 CRUD
- 计划 CRUD
- 日记 CRUD
- 食用者 CRUD
- 推荐
- 库存扣减
- 购物清单
- 上传
- 备份
- 恢复

### 完整闭环测试

```text
新增菜谱
→ 新增库存
→ 推荐成功
→ 加入计划
→ 完成计划
→ 生成日记
→ 扣减库存
→ 缺少食材进入购物清单
→ 统计更新
```

### UI 回归

视口至少包括：

```text
360 × 800
390 × 844
768 × 1024
1280 × 800
1440 × 900
1600 × 1200
```

参考图验收采用：

```text
桌面：1600 × 1200 全页截图
移动：390px 宽全页截图
```

必须检查：

- 项目名为搭饭小馆
- 小羊角色风格一致
- 桌面推荐菜谱横向占满
- 桌面库存概览位于第三行右侧
- 桌面今日饮食记录只有一个
- 桌面第三行为记录、统计、库存三列
- 桌面日历占满底部一行
- 移动快捷入口为两列
- 移动推荐菜谱横向滑动
- 移动库存概览含过期列表
- 移动底栏为首页、厨师、觅食、日记
- 无横向溢出
- 固定导航不遮挡内容

---

## 20. 编码规范

### TypeScript

必须启用严格模式。

禁止滥用：

```text
any
as unknown as
非空断言
忽略类型错误
```

### 后端

- 路由只处理请求和响应。
- 业务逻辑放入 service。
- 输入必须校验。
- 数据访问集中管理。
- 异常统一转换。
- 日志不得记录敏感信息。

### 前端

- API 请求集中在 service。
- 页面组件不承载全部业务逻辑。
- 全局状态只保存跨页面数据。
- 样式统一使用设计变量。
- 禁止重复绑定事件。
- 页面卸载时清理监听器和定时器。
- 禁止使用 `innerHTML` 拼接用户输入。
- 禁止在正式组件中写死演示数据。

---

## 21. AI 功能

AI 不是第一版核心依赖。

没有 AI 时必须正常使用：

- 菜谱
- 店铺
- 食材
- 推荐
- 搭配一顿饭
- 计划
- 日记
- 统计
- 购物清单

AI 标签未配置时，使用本地关键词规则。

API Key 只保存在后端本地配置，前端不得读取完整值。

---

## 22. 禁止事项

禁止：

1. 将项目改成云端应用。
2. 引入 Docker 或 PostgreSQL。
3. 使用 localStorage 代替 SQLite。
4. 只做静态前端。
5. 使用写死数据冒充后端。
6. 在正式逻辑保留 Mock。
7. 跳过 Prisma Migration。
8. 同时并行修改数据库 Schema。
9. 将移动页面直接放大成桌面端。
10. 改变最终首页模块顺序。
11. 把库存概览放回推荐菜谱旁边。
12. 重复显示今日饮食记录。
13. 将推荐菜谱缩回非全宽区域。
14. 使用不同风格的小羊。
15. 实现会员、升级、广告或订阅。
16. 使用专业营养诊断冒充基础统计。
17. 删除到店和外卖功能。
18. 使用“今天吃什么”作为主项目名。
19. 使用“首页、记录、发现、我的”作为移动底栏。
20. 结束所有 Node 进程来停止应用。
21. 未通过首页视觉验收就开始大规模并行开发。
22. 未测试备份就声明恢复功能完成。

---

## 23. 每次任务执行流程

### 修改前

1. 阅读 agent.md。
2. 阅读 PRD 对应章节。
3. 检查仓库状态。
4. 检查数据库结构。
5. 检查 API 契约。
6. 检查参考图和现有截图。
7. 确定最小修改范围。

### 修改后

必须执行：

```text
TypeScript 类型检查
前端测试
后端测试
前端构建
Prisma 迁移验证
API 回归
移动端 UI 回归
桌面端 UI 回归
```

最终汇报必须包含：

- 已完成内容
- 修改文件
- 数据库变化
- API 变化
- 测试命令和结果
- 截图路径
- 未完成内容
- 风险
- 回滚方式

禁止仅汇报“已完成”或“应该没问题”。

---

## 24. 完成判定

一个功能只有同时满足以下条件才算完成：

1. 页面存在。
2. 交互可用。
3. API 已接入。
4. SQLite 已持久化。
5. 刷新后数据存在。
6. 服务重启后数据存在。
7. 桌面端可用。
8. 移动端可用。
9. 有加载、空和错误状态。
10. 有关键测试。
11. 不破坏已有模块。
12. 符合 PRD。
13. 符合本文件。
14. 首页符合最终参考图。

缺少任一项，只能标记为：

```text
部分完成
```

---

## 25. 第一版最终验收

第一版必须证明：

```text
项目主名称为搭饭小馆
桌面和移动端使用同一套小羊角色
新增菜谱后重启仍存在
新增店铺后重启仍存在
新增食材后重启仍存在
推荐条件真实生效
推荐结果可加入计划
计划完成后生成日记草稿
库存可以确认扣减
缺少食材可以进入购物清单
统计来自真实数据库
电脑和手机看到同一份数据
桌面首页推荐菜谱全宽
桌面首页只有一个今日饮食记录
桌面库存概览位于记录和统计同一行的右侧
移动端四个快捷入口为两列
移动底栏为首页、厨师、觅食、日记
自动备份可生成
手动备份可恢复
start.bat 可启动
stop.bat 不误杀其他进程
图片刷新和重启后仍显示
360px 页面无横向滚动
1600×1200 桌面截图符合参考布局
390px 移动全页截图符合参考布局
```

全部通过后，才允许标记第一版完成。
